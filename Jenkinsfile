pipeline {
    /* insert Declarative Pipeline here */
    agent any
    stages {
        stage('Example Build') {
            steps {
                sh 'pwd'
                sh 'ls -lart'
            }
        }
    }
}